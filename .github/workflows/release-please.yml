name: Release Please

on:
  push:
    branches:
      - main

jobs:
  release:
    uses: skjaere/debridav-ci/.github/workflows/reusable-release-please.yml@main
    permissions:
      contents: write
      pull-requests: write
